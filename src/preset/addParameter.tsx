import { PARAM_KEY } from '../constants';

export const parameters = {
    [ PARAM_KEY ]: {
        base: {},
        themes: [{
            name: "Default",
            properties: []
        }]
    },
};
